---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: refineries.gastown.gastown.io
spec:
  group: gastown.gastown.io
  names:
    kind: Refinery
    listKind: RefineryList
    plural: refineries
    singular: refinery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.rigRef
      name: Rig
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.queueLength
      name: Queue
      type: integer
    - jsonPath: .status.currentMerge
      name: Current
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Refinery is the Schema for the refineries API.
          Also known as Crucible in the Olympian API naming convention.
          A Refinery processes merge queues, sequentially rebasing and merging polecat branches.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Refinery
            properties:
              gitSecretRef:
                description: gitSecretRef references the Secret containing git credentials.
                properties:
                  name:
                    description: name is the name of the secret.
                    type: string
                required:
                - name
                type: object
              parallelism:
                default: 1
                description: |-
                  parallelism controls how many merges can be processed concurrently.
                  Default is 1 (sequential processing).
                format: int32
                minimum: 1
                type: integer
              rigRef:
                description: rigRef references the Rig (Forge) to process merges for.
                type: string
              targetBranch:
                default: main
                description: targetBranch is the branch to merge into (e.g., "main").
                type: string
              testCommand:
                description: |-
                  testCommand is the command to run after rebase to validate the branch.
                  If empty, no tests are run.
                type: string
            required:
            - rigRef
            type: object
          status:
            description: status defines the observed state of Refinery
            properties:
              conditions:
                description: conditions represent the current state of the Refinery
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentMerge:
                description: currentMerge is the branch currently being processed.
                type: string
              lastMergeTime:
                description: lastMergeTime is the timestamp of the last successful
                  merge.
                format: date-time
                type: string
              mergesSummary:
                description: mergesSummary provides aggregate merge statistics.
                properties:
                  failed:
                    description: failed is the number of failed merges (conflicts,
                      test failures).
                    format: int32
                    type: integer
                  pending:
                    description: pending is the number of branches waiting in queue.
                    format: int32
                    type: integer
                  succeeded:
                    description: succeeded is the number of successful merges.
                    format: int32
                    type: integer
                  total:
                    description: total is the total number of merges attempted.
                    format: int32
                    type: integer
                required:
                - failed
                - pending
                - succeeded
                - total
                type: object
              phase:
                description: phase indicates the current phase of the Refinery.
                enum:
                - Idle
                - Processing
                - Error
                type: string
              queueLength:
                description: queueLength is the number of branches waiting to be merged.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
