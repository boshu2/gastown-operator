# Promo pipeline repo (in-tree)

Cloned with gastown-operator. Polecat `git-init` checks out this repository; Claude runs the pipeline from:

```
bte-promo-script/repos/bte-promo-script-repo/
```

Update tool files here (workflows, scripts, canon). No separate gastown-static GitHub repo.
