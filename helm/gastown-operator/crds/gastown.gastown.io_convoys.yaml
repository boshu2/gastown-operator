---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: convoys.gastown.gastown.io
spec:
  group: gastown.gastown.io
  names:
    kind: Convoy
    listKind: ConvoyList
    plural: convoys
    singular: convoy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Convoy is the Schema for the convoys API.
          A Convoy tracks a batch of beads being worked on.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConvoySpec defines the desired state of Convoy
            properties:
              description:
                description: Description is a human-readable description of this convoy
                minLength: 1
                type: string
              parallelism:
                default: 0
                description: |-
                  Parallelism controls how many Polecats can run concurrently.
                  Default is 0 (unlimited - run all tasks in parallel).
                format: int32
                minimum: 0
                type: integer
              rigRef:
                description: RigRef references the Rig where Polecats will be created.
                type: string
              trackedBeads:
                description: TrackedBeads is the list of bead IDs to track
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - description
            - trackedBeads
            type: object
          status:
            description: ConvoyStatus defines the observed state of Convoy
            properties:
              completedAt:
                description: CompletedAt is when the convoy completed
                format: date-time
                type: string
              completedBeads:
                description: CompletedBeads is the list of beads that have been completed
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the current state of the Convoy
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              pendingBeads:
                description: PendingBeads is the list of beads still in progress
                items:
                  type: string
                type: array
              phase:
                default: Pending
                description: Phase is the current lifecycle phase
                enum:
                - Pending
                - InProgress
                - Complete
                - Failed
                type: string
              progress:
                description: Progress is a human-readable progress indicator (e.g.,
                  "2/3")
                type: string
              startedAt:
                description: StartedAt is when the convoy started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
