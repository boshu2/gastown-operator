---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: witnesses.gastown.gastown.io
spec:
  group: gastown.gastown.io
  names:
    kind: Witness
    listKind: WitnessList
    plural: witnesses
    singular: witness
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.rigRef
      name: Rig
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.polecatsSummary.running
      name: Running
      type: integer
    - jsonPath: .status.polecatsSummary.stuck
      name: Stuck
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Witness is the Schema for the witnesses API.
          Also known as Sentinel in the Olympian API naming convention.
          A Witness monitors Polecat health in a Rig and escalates anomalies.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Witness
            properties:
              escalationTarget:
                default: mayor
                description: escalationTarget specifies where to send alerts (e.g.,
                  "mayor", "slack", "email").
                type: string
              healthCheckInterval:
                default: 30s
                description: healthCheckInterval specifies how often to check polecat
                  health.
                type: string
              rigRef:
                description: rigRef references the Rig (Forge) to monitor.
                type: string
              stuckThreshold:
                default: 15m
                description: stuckThreshold specifies how long a polecat can be idle
                  before being considered stuck.
                type: string
            required:
            - rigRef
            type: object
          status:
            description: status defines the observed state of Witness
            properties:
              conditions:
                description: conditions represent the current state of the Witness
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastCheckTime:
                description: lastCheckTime is the timestamp of the last health check.
                format: date-time
                type: string
              phase:
                description: phase indicates the current phase of the Witness.
                enum:
                - Pending
                - Active
                - Degraded
                type: string
              polecatsSummary:
                description: polecatsSummary provides a summary of polecat states
                  in the monitored rig.
                properties:
                  failed:
                    description: failed is the number of failed polecats.
                    format: int32
                    type: integer
                  running:
                    description: running is the number of actively running polecats.
                    format: int32
                    type: integer
                  stuck:
                    description: stuck is the number of polecats that appear stuck
                      (no progress).
                    format: int32
                    type: integer
                  succeeded:
                    description: succeeded is the number of successfully completed
                      polecats.
                    format: int32
                    type: integer
                  total:
                    description: total is the total number of polecats in the rig.
                    format: int32
                    type: integer
                required:
                - failed
                - running
                - stuck
                - succeeded
                - total
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
